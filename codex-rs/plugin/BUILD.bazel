load("//:defs.bzl", "crewon_rust_crate")

crewon_rust_crate(
    name = "plugin",
    crate_name = "crewon_plugin",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
