load("//:defs.bzl", "crewon_rust_crate")

crewon_rust_crate(
    name = "core-plugins",
    crate_name = "crewon_core_plugins",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
