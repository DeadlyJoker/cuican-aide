load("//:defs.bzl", "crewon_rust_crate")

crewon_rust_crate(
    name = "skills",
    crate_name = "crewon_skills",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
