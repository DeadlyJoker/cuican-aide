load("//:defs.bzl", "crewon_rust_crate")

crewon_rust_crate(
    name = "goal",
    crate_name = "crewon_goal_extension",
    compile_data = glob([
        "templates/**",
    ]),
    integration_compile_data_extra = ["src/accounting.rs"],
)
