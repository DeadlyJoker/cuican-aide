load("//:defs.bzl", "crewon_rust_crate")

crewon_rust_crate(
    name = "features",
    crate_name = "crewon_features",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
