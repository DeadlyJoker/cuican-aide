# W3-03 Implementation Plan

## Stage K: State migration journal and fence kernel

1. 添加 `0054_office_migration_journal.sql`，约束 phase、CAS revision、source/snapshot digest、Workspace FK 和硬上限。
2. 添加独立 records/runtime/test 模块；先验证 duplicate、conflict、restart、tamper、bounds。
3. 不修改 Office 热点和生产 dispatch。

## Stage I: Bounded legacy importer

1. 建立真实历史 fixture normalization 和 semantic equality Harness。
2. 在独立 app-server test module 完成固定目录读取、source observation、quiesce 判定、CAS source advance 与 opaque snapshot commit 的 executable specification。
3. 对 active/unknown Run 停在 quiescing；不复制内存状态，不静默接受受控终止产生的 source drift。
4. 等 Stage F 证明所有旧 authority 入口都受 fence 保护后，再把 importer 接入 production composition，并保持 admission 默认关闭。

## Stage F: Legacy fence composition

1. F1a：v2 Office RPC 主 record create/save/delete 在 authority lock 内检查 State fence，并封堵 id-less legacy 文件复制绕过。
2. F1b：移除内部生产路径的 legacy-unfenced processor 构造，确保所有主 record mutation 使用同一 State handle。production state injection 已 Green，`state_db=None` in-process migration fence Harness 也已 Green。
3. F2a：在统一 processor 中建立 `NewWork`/`DrainExisting` 分类，让 run/retry、delegation/verification prepare、auto-dispatch queue/claim 与 terminal/failure/cancel reconcile 分别执行对应 fence；completion monitor/history recovery 也改用同一 processor。（首轮接线已完成，未 Green）
4. F2b（P0）按可审查阶段推进：
   - F2b-1（已完成）：linear `OfficeDispatchPermit` 持有 Office authority，已跨 run/delegation/verification 的 prepare、真实 `turn/start`、Automation run record 到 started/failed/retryable commit。
   - F2b-2（已完成）：scheduler exact `intentId` 预阶段。typed `OfficeAutoDispatchIntentRef` 已进入 live/startup 路径，queue 稳定复用同一 source pair 的 `intentId`：claim/mark/clear 同时匹配 `intentId` + source pair + lease + `dispatching`，requeue 保守处理，clear 只删 exact row。scheduler 9/9 与 sync/delegation/replan focused Harness 各 1/1 Green；duplicate-row、old-id、failed/requeue 和 concurrent-claim review findings 已修复。
   - F2b-3（Red）：stable `officeRecordId`、dispatch receipt、client message identity 与 scheduler exact intent/permit/core Turn 的绑定及 targeted Office restart/reclaim Harness 已落地；v2 Durable Execution Fence 进一步把 core 顺序固定为 `v2 admission flush -> matching executionFence flush -> task start`。这只关闭了“已 durable admission、尚未越过执行边界”的可判定窗口，仍需 downstream model/tool/MCP idempotency/reconcile、真实 power-loss/fsync、多文件故障与迁移竞态证明完整 exactly-once 外部副作用。
   - P0b-1 durable marker/resume index（Green）：`UserInputOnceMarker`、bounded chronological resume index、fork fail-closed 和 legacy collision 已落地；marker 对模型上下文不可见。v2 admission 与 matching v2 `executionFence` 均受 exact thread/client/payload/turn identity约束；v1、unknown phase、mismatch/out-of-order fence 和容量溢出均 fail-closed。
   - P0b-2b default-off idle admission receipt（targeted Green）：`submit_user_input_once` 与普通 submission 共用单一 FIFO consumer；同一 client identity 并发只写一个 `AdmissionOnly` receipt，不同 payload hash conflict，重启后恢复 receipt；regular/once identity 双向 collision fail-closed。只有 exact v2 `AdmissionOnly + executionState=notStarted` 且 rollout 中无 matching UserMessage/turn lifecycle evidence时，Office 可通过显式 `StartIfNotStarted` 恢复原 turn；普通 lookup 不自动执行。writer 仅在 `user_input_once` UnderDevelopment feature 显式启用时可达，active turn、thread-setting override、final-output schema、Responses metadata 和 additional context 均拒绝。该 stacked review slice churn 442。
   - P0b-2c reservation-aware task start（targeted Green）：共享 task-start authority 在同一原子边界校验 exact reservation、登记可取消 `RunningTask`，spawn 由 start gate 阻挡到登记完成；reservation 失配或 gate 后已取消时不 drain captured pending、不发 lifecycle、不启动模型。普通 `start_task` 的原有初始化顺序保持不变。该独立 stacked slice churn 314。
   - P0b-2d turn-context precondition（targeted Green）：`submit_user_input_once` 新增显式 `TurnContextPrecondition`；新 admission 先捕获同一份 SessionConfiguration + resolved environments，按 primary environment cwd（否则 fallback cwd）校验 expected cwd，admission与execution fence均 flush 后再用该快照 materialize TurnContext。mismatch 不消费 client identity，exact replay 在 cwd 改变后仍优先返回已有 receipt。
   - F2b-3d2 identity/receipt production contract（targeted Green）：Office-owned versioned identity 使用 bounded client ID、64 位 lowercase payload hash 与 bounded exact `promptSnapshot`；delegation 内嵌 `dispatchReceipt` 的 `starting -> admitted/failed` 转换、exact physical-path reload/CAS、server-owned generic-save protection 与 legacy fail-closed fence 已进入 production 编译树。gate-off legacy 1/1、gate-on live admission 1/1，identity/receipt/recovery targeted 30/30 Green；receipt writer persistence seam 4/4 与 locator unknown-authority fail-closed 6/6 也已 targeted Green。
   - F2b-3d3 restart/reclaim evidence（targeted Green，整体仍 Red）：`starting` restart + expired child/scheduler lease reclaim、admitted scheduler-sidecar restart、受控 after-commit/no-marker restart、legacy v1 marker quarantine、exact v2 admission-no-fence resume、Office `executionUnknown` 已提交但 scheduler 未提交的单边故障收敛、exact active attach，以及 gate-off drain fixture 均精准 Green。current-runtime ownership 覆盖 task active 与 terminal event 持久化前的 finishing/aborting 窗口；active attach Harness 使用请求进入信号与响应 gate，未用 sleep 判断活跃态；partial-commit 二次重启通过同步 `office/read` 完成 recovery pass，并断言 recovery server总请求为零。v2 recovery仅在 exact admission、无 fence、无 UserMessage/lifecycle evidence时显式恢复原 turn；v1/unknown phase、已有 UserMessage/lifecycle但缺 fence、或已有 fence却无 exact active/terminal证据时均 fail-closed。受控 fixture 不证明所有生产 crash window 或外部 execution 状态都可判定。
   - F2b-3d4 Durable Execution Fence recovery matrix（targeted Green，整体仍 Red）：恢复路径把 v2 `executionFence` 视为“task 即将开始”的 durable边界，而非外部副作用完成凭证。exact current-runtime active/terminalizing turn只 attach monitor，ThreadStore 中 exact persisted terminal turn只 reconcile；越过 fence但缺少上述证据、legacy v1 admission、unknown/mismatched phase 或 persisted UserMessage/lifecycle无 fence均持久化 `executionUnknown`、清除 child/scheduler lease、终结自动重试。唯一可重启执行的是 exact v2 admission-only/no-fence/no-execution-evidence，由 Office 显式调用 guarded resume。该分层 fail-closed 语义不等于 external exactly-once authority。
   - F2b-3d5 writer fault Harness（targeted Green）：使用 test-only `ThreadStore` fault port 覆盖 admission/fence append、flush 与 history load 的 exact/absent/unknown 组合。7/7 Green，Nextest run `11def95f-63ba-4b8a-bc0d-d83f383c5154`；resume index 对 duplicate/unknown/wrong-thread marker 全部 poison 并 13/13 Green，run `506976a8-a4a7-4efa-a718-a9d764f9261b`。durable fence ack 丢失返回 `ExecutionStartLost`，history 不可判定返回 `LegacyUnknown`，模型请求均为零；只有 exact admission/no fence允许 guarded resume。这不是真实文件系统 fsync/power-loss 证明。
   - F2b-3d6 single-writer lease/handoff（targeted Green，release gate仍 Red）：per-thread OS lease 已绑定 live writer 完整生命周期，并扩展到 cold metadata、compression、archive/unarchive/delete；shutdown/discard 等待 writer task 退出后才返回。resume 在 lease 内重读完整 durable `RolloutItem` 流，thread id、parse errors 与 exact history 任一不满足即 fail-closed。真实双 app-server graceful handoff、异常退出释放和core conflict mapping已Green。generation lock substrate已实现 `LegacyFenceExclusive`/`LeaseAwareShared`：lock matrix 6/6（run `435432c6-bd5e-4dc1-8113-ca4f430d90c3`）、rollout 84/84（run `542733ca-066a-4c2c-aa53-7939373800f1`）、default app-server mode 2/2（run `bd8a7a23-2b30-4743-a1e7-cf80128f39f0`）、legacy feature mode 2/2（run `a1b67eea-3d17-4f04-9cdd-ffa839fac47c`）。F2b-3d6c仍须按四个可审查阶段推进：
     - Stage A（targeted/full crate Green）：Legacy artifact 对 marker-bearing/unreadable rollout 的 resume/append/metadata/compression mutation fail-closed，marker-free ordinary rollout保持可用；default 89/89、legacy feature 94/94 Green。
     - Stage B（targeted/full crate Green）：ThreadStore SQLite-only metadata 与 archive/unarchive/delete 在任何 DB/index/file mutation 前完成同lease admission，多候选delete先全部预检；default 93/93、legacy feature 99/99 Green。
     - Stage C（targeted Green）：legacy artifact编译配置force-off `UserInputOnce` 与 `OfficeAutoDelegationDurableAdmission`，运行时env/config不能重新开启。
     - Stage D（build/probe substrate已实现，production wiring仍Red）：builder默认拒绝dirty tree，在`codex-rs` pinned toolchain下使用`--frozen --release --no-default-features`和两个隔离`CARGO_TARGET_DIR`构建lease-aware/legacy artifacts；构建前后绑定同一`Cargo.lock` SHA与完整Git HEAD/status快照，在临时stage中复制为只读artifact并分别生成两套manifest/allowlist，全部验证后才使用macOS/Linux原子no-replace rename发布；所有子进程具有分类硬超时和进程组清理。black-box probe不信manifest或binary自报generation，候选binary只获得最小环境白名单，共享home使用有界内容/元数据/SHA快照，且必须验证exact四种case：`leaseAware+leaseAware`双进程distinct ports均initialize，以及`legacy+legacy`、`legacyOwner+leaseAwareContender`、`leaseAwareOwner+legacyContender`均在State/listener前conflict；builder还必须逐一比对probe回传的artifact path/SHA/size与刚构建identity。该substrate的receipt仍为`authority=non-authoritative`、`productionWiring=notConnected`，Python verifier仍有verify/exec TOCTOU且不是production authority。当前dirty tree下没有构建真实release，三个Legacy相关真实release conflict case也未执行；production binary/container authority与macOS/Linux/container atomic launcher方案尚未选择，因此Stage D不得标完成。
     generation lock本身不能阻止不合作的pre-fence binary；真实 power-loss/fsync 与 NFS/非本地锁语义也仍未验证。
   - F2b-3d7 executionUnknown reconcile（设计完成，实现 Red）：拟议 v2 `office/delegation/reconcile` 必须 server-owned、exact identity/CAS、只读执行权威且不启动 turn；不接受 client config、filePath 或 turnId。只有 canonical receipt/scheduler/runtime/thread 给出 exact active 或 terminal evidence 时才可收敛为 attached/completed/failed/interrupted；证据不足必须保持 stillUnknown，UI 的“重新检查状态”不能隐式变成 retry。
   - P0b-3 production activation（Red）：上述 targeted Office restart/reclaim/downgrade/partial-commit、latest combined durable 9/9、writer fault 7/7、真实双 app-server graceful handoff、异常退出 lease 释放、generation lock targeted Green与`crewon-app-server` 1252/1252 full suite Green仍不等于整体Gate Green。F2b-3d6c的Stage A-C已闭合，Stage D build/probe substrate也已实现，但clean-tree真实release、三个Legacy相关真实release conflict probe、production binary/container authority与atomic verify+exec launcher/container SHA wiring仍未闭合；standalone verifier 10/10且`productionWiring:notConnected`不能关闭deployment Gate。external execution authority、downstream model/tool/MCP idempotency/reconcile、真实power-loss/fsync及NFS语义、其余receipt/rollout/scheduler多文件故障矩阵、migration-vs-dispatch竞态、storage/memory sidecar bounds与最终core/workspace full suite仍未闭合；durable Office feature必须继续default-off。
5. F2c（P0）：把 `DrainExisting` 收紧为 migration-owned reconcile；只允许对 journal 中已记录的 active identity 做 terminal/cancel 收尾，拆除 terminal sync 内部的隐式 scheduler enqueue，并在持有 Office authority 时重读 source 后 CAS advance journal。
6. F2d（P1）：重排 scheduler recovery/runtime repair：先对具体 Office/source turn 获取 stable claim/receipt/permit，再 load/repair member 或 Automation runtime；验证 fenced Office 不会创建 replacement Thread、改 Agent/Automation 或启动 verification/replan。existing-member、empty-member repair 与 missing-Automation repair Harness 均 isolated 3/3 Green；auto-replan Harness 补入 pending scheduler intent 后也 isolated 3/3 Green。兼容性修复后、这些 Harness 最终改动前的 full suite 为 1187 项中 1181 passed、3 failed、3 timeout；唯一 Office failure 为旧 replan Harness，现已有后续 isolated 修复证据，其余为既有 thread/remote/session 资源时序。最终改动后的 full-suite 仍待补证，且 stable claim/receipt/permit 依赖 F2b-3，因此 F2d 不标完成。
7. F3-0（串行前置）：先建立唯一 `OfficeStorageAuthority` facade，把 migration fence、authority lock、record lock 与物理 main-record backend 决策收口；prototype 只能先以只读/parity 方式接线，禁止逐模块启用 writer 形成双 authority。
8. F3-1（可并行准备）：分别完成 read/discovery/identity、run/scheduler sidecar、memory sidecar、message receipt/snapshot、runtime/workspace sidecar 与 message/member/manager parity Harness；所有 store 都必须有 byte/item 硬上限、原子替换和 corrupt fail-closed。
9. F3-2（串行切换）：一次性把 processor、run、memory、startup/runtime repair 和 scheduler 最终 mutation 的 Office main-record 读写切到 facade，并删除业务层可达的通用 Office writer。
10. F3-3（再并行）：在单一物理 writer 已成立后切换 message、member/manager/runtime、run/orphan dispatch 与 automation binding 行为模块。
11. F3 Harness：`rg` + kernel/integration 测试证明没有绕过 facade/authority lock/permit 的生产调用点，并证明同 cwd 的未迁移 Office和独立 Automation 不被粗粒度误阻断。

## Stage V: Verification and handoff

运行 `crewon-state`、app-server targeted migration/fence tests；完成 scoped fix，最后 `just fmt`。代码审查要求保持可审查 wave，不将整个 W3-03 或 permit + recovery + sidecar 作为一个落地单元。后续顺序为：

1. 保留 writer fault 7/7、resume-index 13/13、真实双 app-server graceful handoff、异常退出 lease 释放、generation lock 6/6与rollout 84/84作为persistence基线。Stage A-C已闭合；Stage D先在clean tree上用builder生成两套真实release并完成exact四case probe/identity复核，再选择并实现production authority与atomic verify+exec launcher/container wiring。standalone Python verifier、builder receipt和probe都不能替代production launcher；不要把`productionWiring:notConnected`误写成pre-fence兼容性保证。随后实现不启动turn的`executionUnknown` exact reconcile，再补downstream model/tool/MCP idempotency/reconcile、真实power-loss/fsync与NFS语义、多文件fault injection、migration竞态和release activation gate。
2. 完成 F2c migration-owned reconcile 与 source CAS advance。
3. 在现有 isolated 3/3 Green 基础上，补跑最终 Harness 改动后的 full suite，并在 F2b-3 stable claim/receipt/permit 闭合后完成 F2d runtime repair 顺序证明。
4. 先完成 F3-0 storage authority facade，再并行做 F3-1 sidecar/parity 准备；只有 F3-2 main-record 单一 writer cutover 后才能切行为模块和启用 strict quarantine。
5. 最后完成 I2b production importer composition/admission，并保持默认 disabled 直到全部 Gate Green。

W3-04 只能在上述顺序完成后消费 `imported` snapshot，不能重新读取旧 JSON 作为 authority。
