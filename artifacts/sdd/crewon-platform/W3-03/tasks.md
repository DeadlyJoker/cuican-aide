# W3-03 Tasks

- [x] D1 盘点 Office 主记录、dispatch、scheduler 和独立 sidecar 写入口。
- [x] D2 冻结单向 phase、Authority、snapshot 与无双写边界。
- [x] K1 Red Harness：duplicate/conflict/restart/tamper/bounds。
- [x] K2 `0054_office_migration_journal.sql` 与 records/runtime kernel。
- [x] K3 State targeted + project tests Green。
- [x] I1 真实历史 Office fixtures 与规范化 equality Harness。
- [x] I2a bounded importer、crash recovery、quiesce stop 与 CAS source advance executable Harness。
- [ ] I2b importer production composition 与默认关闭的 admission。
- [x] F1a v2 Office RPC 主 record create/save/delete authority lock + State fence。
- [x] F1b 内部生产 processor 的主 record mutation 全部注入同一 State fence；State 不可用时 fail-closed；`state_db=None` in-process migration fence Harness Green。
- [x] F2a 首轮运行接线（wiring complete，Gate 仍 Red）：`NewWork`/`DrainExisting` 分类，run/retry/cancel、delegation/verification prepare/terminal mark、auto-dispatch queue/claim 与 completion/history sync 经由同一迁移感知 processor。
- [x] F2b-1 linear `OfficeDispatchPermit`：跨 run/delegation/verification prepare、真实 turn/automation start 到 started/failed/retryable commit。
- [x] F2b-2 scheduler exact `intentId` 预阶段：typed `OfficeAutoDispatchIntentRef` 贯通 live/startup，queue identity 稳定，claim/mark/clear 校验 `intentId` + source pair + lease + `dispatching`，requeue 保守，clear exact row。scheduler 9/9 与 sync/delegation/replan focused Harness 各 1/1 Green，duplicate-row、old-id、failed/requeue、concurrent-claim findings 已修复。
- [x] F2b-3a P0b core durable marker/resume index + v2 execution fence：bounded marker、chronological first-authority、fork/legacy fail-closed；写入顺序固定为 `v2 admission flush -> matching executionFence flush -> task start`，fence不进入模型上下文。
- [x] F2b-3b P0b default-off idle AdmissionOnly receipt：FIFO、concurrent duplicate 单模型请求、hash conflict、regular/once collision；仅 exact v2 `AdmissionOnly`、无 execution fence且无 matching UserMessage/lifecycle evidence可由 Office显式 guarded resume。v1、unknown phase、persisted-without-fence、fenced-without-exact-active/terminal均 fail-closed。
- [x] F2b-3c P0b reservation-aware task start：exact reservation 原子挂载、start gate、失配/取消不 drain/emit/start，以及 success/mismatch/cancel Harness；targeted 3/3 Green，stacked churn 314。
- [x] F2b-3d1 P0b turn-context precondition：同一 configuration/environment 快照校验 effective cwd 并创建 TurnContext；mismatch 不消费 identity，cwd 改变后 exact replay 仍返回已有 receipt。matching-turn exact 本轮 1/1 Green（run `1a42fa6b-828d-47c3-93ea-af20bf224b0b`）；完整 `user_input_once` 22 项本轮为 17 passed/5 项既有 10s timeout（run `cae240b6-9bf0-4f42-b635-16ffb489623d`），不记为 full Green。
- [x] F2b-3d2 Office identity/receipt contract：bounded stable client identity、canonical payload hash、exact `promptSnapshot` 与 delegation 内嵌 `dispatchReceipt starting -> admitted/failed` 状态机已进入 production 编译树；receipt/identity/recovery targeted 30/30 Green，generic `office/save` 已保护 server-owned durable fields。
- [ ] F2b-3d3 P0 stable dispatch authority：default-off 双 gate、真实 Auto Delegation permit/core v2 admission/execution fence、exact physical-path recovery 与 admitted scheduler sidecar CAS 已接线。receipt writer seam 4/4、locator unknown-authority fail-closed 6/6、gate-off/live、`starting`/admitted/after-commit、exact v2 admission recovery、v1 quarantine、exact active attach与 Office-first scheduler-lagging partial commit均 targeted Green；最新 combined durable filter 9/9 Green（run `d88fd93d-a716-4a96-b4f8-0064bc35c39b`）。core writer fault 7/7、resume index 13/13、真实双 app-server graceful handoff、异常退出lease释放、writer conflict映射、generation lock、Legacy marker mutation fence、legacy feature force-off 与双 generation startup Harness均Green。仍缺F2b-3d6c Stage D immutable production artifact/launcher/container SHA wiring、downstream model/tool/MCP idempotency/reconcile、真实power-loss/fsync及NFS语义、其余多文件故障矩阵、migration竞态、storage/memory边界和最终core/workspace full suite；durable feature不得production enable。
- [x] F2b-3d4 Durable Execution Fence recovery matrix：exact v2 admission-only/no-fence/no UserMessage-or-lifecycle evidence可显式恢复原 turn；legacy v1 marker、unknown/mismatched phase、persisted UserMessage/lifecycle无 fence、以及有 fence但无 exact current-runtime active/terminalizing或 persisted terminal证据均持久化 `executionUnknown` 并停止自动 replay。新增 writer fault Harness 证明 execution fence 已 durable 但 ack 失败时返回 `ExecutionStartLost`，模型请求为零，普通 replay 与 `StartIfNotStarted` 都不会再执行。该切片只提供可判定的 resume边界与 fail-closed隔离，不代表 external exactly-once 或 overall Green。
- [x] F2b-3d5 writer fault targeted Harness：覆盖 admission/fence append reject、flush 后 exact/absent/unknown、durable fence ack lost 与 history load failure；只有 exact admission/no fence 可 guarded resume，fence exact/unknown 均 fail-closed。7/7 Green，run `11def95f-63ba-4b8a-bc0d-d83f383c5154`；duplicate/unknown/wrong-thread marker poison 13/13 Green，run `506976a8-a4a7-4efa-a718-a9d764f9261b`。
- [ ] F2b-3d6 双进程 single-writer lease + handoff（umbrella；release Gate仍 Red）。
  - [x] F2b-3d6a per-thread OS lease覆盖 create/resume、flush/shutdown/discard、cold metadata、compression、archive/unarchive/delete；resume在lease内重读并exact校验durable history，WouldBlock fail-fast。generation lock matrix 6/6 Green（run `435432c6-bd5e-4dc1-8113-ca4f430d90c3`）；新增generation substrate后的`crewon-rollout` 84/84 Green（run `542733ca-066a-4c2c-aa53-7939373800f1`）。
  - [x] F2b-3d6b 两个真实 app-server 共享同一 `CREWON_HOME` 的 resume/append/graceful handoff Harness：并发 owner 返回 typed invalid-request，旧 owner shutdown 后新 owner 接管，最终 marker 各恰好一次。Green run `17429c41-974e-4d93-becc-93f99c97a8f6`；异常退出 lease 释放 Green run `5c67d54e-2882-43c3-8110-fdb40c7764fb`；core conflict mapping Green run `d032f79a-2c0b-41e8-8b76-3c496ee95e50`。
  - [ ] F2b-3d6c 两阶段旧版本围栏（umbrella；仍未完成）。generation lock substrate已证明`LegacyFenceExclusive`与`LeaseAwareShared`的共享/排他、canonical-home、跨进程与kill释放语义；app-server默认mode 2/2 Green（run `bd8a7a23-2b30-4743-a1e7-cf80128f39f0`），legacy feature mode 2/2 Green（run `a1b67eea-3d17-4f04-9cdd-ffa839fac47c`）。这些只覆盖fence-aware binary。
    - [x] Stage A rollout boundary：Legacy artifact 对 marker-bearing/unreadable rollout 的 resume/append/metadata/compression mutation fail-closed，marker-free ordinary rollout保持可用；default 89/89、legacy feature 94/94 Green。
    - [x] Stage B ThreadStore boundary：SQLite-only metadata 与 archive/unarchive/delete 在任何 DB/index/file mutation 前完成同lease admission；read/list保持可用，多候选delete先全部预检。default 93/93 Green（run `ff987062-9429-4aba-8381-6e270c53b8e4`），legacy feature 99/99 Green（run `9884e69c-34cb-499e-8f2f-797b1475b288`）。
    - [x] Stage C legacy feature force-off：`legacy-fence-artifact` 编译时强制关闭 `UserInputOnce` 与 `OfficeAutoDelegationDurableAdmission`，配置显式开启也不可绕过；targeted 1/1 Green（run `3d963730-7214-4348-8569-751c80bb3fe7`）。
    - [ ] Stage D artifact build + production wiring（umbrella；仍Red）。
      - [x] Stage D1 builder/probe substrate：默认拒绝dirty tree；使用`codex-rs/rust-toolchain.toml` pinned toolchain、`cargo --frozen --release --no-default-features`、两个隔离`CARGO_TARGET_DIR`；绑定构建前后`Cargo.lock` SHA以及完整Git HEAD/status快照；临时stage完成两artifact、两manifest、两allowlist和non-authoritative receipt后用macOS/Linux原子no-replace rename发布。builder子进程具有分类硬超时和进程组清理。probe强制exact四case并由builder核对case set/result、distinct ports及两artifact path/SHA/size identity；候选binary只获得最小环境白名单，共享home使用有界内容/元数据/SHA快照并严格回收进程组。三个Python unittest文件合计39/39 Green且`py_compile`通过；`--allow-dirty --dry-run`确认Rust 1.95.0、`aarch64-apple-darwin`、`codex-rs` cwd及两条frozen release命令，默认权威构建在dirty tree下按预期拒绝。
      - [ ] Stage D2 clean-tree真实release与probe：当前workspace dirty，真实Legacy Fence/lease-aware release尚未构建；`legacy+legacy`、`legacyOwner+leaseAwareContender`、`leaseAwareOwner+legacyContender`三组真实release conflict probe未跑，不得用fake probe或unit runner代替。
      - [ ] Stage D3 production authority + atomic launcher/container：选择macOS/Linux/container真实immutable artifact authority，在同一受控launcher中完成verify+exec或由runtime按OCI digest启动；禁止Python verify后按path exec的TOCTOU。standalone verifier与builder receipt均为`productionWiring:notConnected`，不能关闭SHA Gate。
  - [ ] F2b-3d6d 真实 power-loss/fsync 与 NFS/非本地文件系统锁语义 Harness；进程 kill/abrupt exit 的 lease 释放已 Green，但不能替代断电 durability 证明。
- [ ] F2b-3d7 `executionUnknown` reconcile：拟议 v2 `office/delegation/reconcile`，server-owned、不启动 turn，不接受 client config/filePath/turnId；只能依 canonical receipt/scheduler/runtime/thread terminal evidence 转为 attached/completed/failed/interrupted，证据不足保持 stillUnknown。未实现，不得将“重新检查”变成重试执行。
- [ ] F2c P0 migration-owned reconcile：限定 drain owner/identity，拆除 terminal sync 隐式 enqueue，收尾后重读 source 并 CAS advance journal。
- [ ] F2d P1 runtime repair：scheduler/verification recovery 先 stable claim/receipt/permit，再创建 replacement Thread 或修改 Agent/Automation/runtime binding。existing-member、empty-member repair、missing-Automation repair 与补齐 pending intent 后的 auto-replan Harness 均 isolated 3/3 Green；最终 Harness 改动后的 full-suite 尚未补跑，且 stable claim/receipt/permit 仍由 F2b-3 阻塞。
- [ ] F3-0 串行 storage authority facade：唯一 backend 决策、migration fence、authority/record lock；prototype 先只读/parity，不启用第二 writer。
- [ ] F3-1a read/discovery/recordId parity；F3-1b run/scheduler bounded store；F3-1c memory recordId/bounds；F3-1d receipt/snapshot store；F3-1e runtime/workspace store；F3-1f message/member/manager parity（1a-1f 可按文件 owner 并行）。
- [ ] F3-2 串行 main-record cutover：processor/run/memory/startup/thread/scheduler 的 Office 读写全部经 facade，业务层通用 Office writer `rg` 为零。
- [ ] F3-3a message、F3-3b member/manager/runtime、F3-3c run/orphan、F3-3d automation binding 行为切换（F3-2 后可并行）。
- [ ] F3-4 strict corrupt quarantine + 全量 bounds/fence Harness，证明独立 Automation/未迁移 Office 不被误阻断。
- [ ] V1 scoped tests/fix/fmt 与 verification 更新。
